<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
           http://www.liquibase.org/xml/ns/dbchangelog
           http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="4" author="hamza">

        <createTable tableName="mouvement_stock">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="date_mouvement" type="DATETIME">
                <constraints nullable="false"/>
            </column>

            <column name="type" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="quantite" type="DECIMAL(15,2)">
                <constraints nullable="false"/>
            </column>

            <column name="cout_unitaire" type="DECIMAL(15,2)">
                <constraints nullable="false"/>
            </column>

            <column name="commentaire" type="VARCHAR(255)"/>

            <column name="produit_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_mouvement_produit"
                             references="produit(id)"/>
            </column>

            <column name="commande_id" type="BIGINT">
                <constraints nullable="true" foreignKeyName="fk_mouvement_commande"
                             references="commande_fournisseur(id)"/>
            </column>

        </createTable>

    </changeSet>

</databaseChangeLog>
